// Backward-compatible campaign adapter. Canonical Content Composer schema lives
// in lib/content so future domains do not depend on newsletter-owned paths.
export * from "@/lib/content/schemas/landing-blocks";
