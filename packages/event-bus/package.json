{
  "name": "@panta/event-bus",
  "version": "0.1.0",
  "private": true,
  "description": "Panta Event Bus — tipizovani cross-engine kontrakti + tanki in-process fan-out (subscribe/publish, error-izolovan). Bez baze/Next/React. T8: prvih 5 event kontrakata.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.1.9"
  }
}
